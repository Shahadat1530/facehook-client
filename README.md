# Future Works-

- Need to add Edit functionality.
- Right now using lws provided backend, but need to create own backend.
- Wanna change UI more better.
- Need to add functionality for check others profile.
- Add Firebase for login/register, along with google login.
- Their are others error which need to fixed
- Also need to fix the Auth, as if users refresh the window/tab then user redirect to login page.
- Other things which will be working when will touched.
